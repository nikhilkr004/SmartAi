services:
  - type: web
    name: studyai-backend
    runtime: node
    plan: starter
    region: singapore
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
